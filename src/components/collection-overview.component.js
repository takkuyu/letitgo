import React from 'react';
import { Link } from 'react-router-dom';

const CollectionOverview = ({ type, items }) => {
    return (
        <div className="collection-overview products" >
            {/* <h2>{type}</h2>
            <Row className="products-container">
                {this.postingList()}
            </Row> */}
        </div >
    );
}

export default CollectionOverview;
